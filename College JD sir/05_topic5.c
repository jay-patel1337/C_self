#include <stdio.h>

int main(){
    char a = 65;
    float b = 5;
    // float c = 6;
    for (int i = 65; i < 91; i++)
    {
        printf("%c", i);
    }
    
    printf("%c", a);
    // printf("%f", (a+b));
    // printf("%d %d %d", sizeof(a), sizeof(b), sizeof(c));

    return 0;
}