#include <stdio.h>

int main()
{
    int i = 0;
    while (i < 10)
    {
        printf("the value of i is %d \n", i);
        i++;
    }
    return 0;
}
// if condition is true while code slab will be executed
// in last addition of value is necessary (In this case)
// Universal truth with automatically satisfy the condition and start running code infinitely
